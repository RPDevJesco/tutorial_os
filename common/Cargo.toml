[package]
name = "common"
version = "0.1.0"
edition = "2021"
description = "Shared no_std foundations for Tutorial-OS (MMIO primitives, types)"

# No dependencies — this is the bottom of the dependency graph.

[lib]
name = "common"
path = "src/lib.rs"
