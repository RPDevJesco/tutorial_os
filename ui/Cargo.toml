[package]
name = "ui"
version = "0.1.0"
edition = "2021"
description = "UI system (canvas, themes, widgets) for Tutorial-OS"

[dependencies]
hal     = { path = "../hal" }
drivers = { path = "../drivers" }
